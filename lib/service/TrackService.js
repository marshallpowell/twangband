
var Q = require('q');
var keystone = require('keystone');
var logger = require(APP_LIB + 'util/Logger').getLogger(__filename);
var authUtils = require(APP_LIB+'util/AuthUtils');

var TrackService = module.exports = {};

TrackService.saveTrack = function(reqBody, reqFiles){
    logger.debug("enter save track");



}
